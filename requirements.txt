# CMSE443 Quadcopter Simulator - Python Dependencies
# Install with: pip install -r requirements.txt

# Tornado (MUST be 4.5.3 - msgpack-rpc-python requires tornado<5)
# DO NOT CHANGE - cosysairsim depends on msgpack-rpc-python
tornado==4.5.3

# msgpack RPC (required by cosysairsim)
msgpack-rpc-python

# Cosys-AirSim Python Client
cosysairsim

# Core dependencies
numpy>=1.21.0

# Xbox Controller Support (Windows)
XInput-Python>=0.4.0

# Keyboard Input (fallback)
keyboard>=0.13.5

# Async support
nest_asyncio

# GUI (usually included with Python)
# tkinter - included in standard library

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: For better timing on Windows
# pywin32>=305

